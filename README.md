
# Установка сервера Xray с поддержкой TLS и Reality

## Описание

Скрипт `install_xray.sh` автоматически разворачивает Xray-сервер с поддержкой **двух протоколов**:

### 1. **VLESS + TLS** (порт 443)
- Полноценный TLS с сертификатами Let's Encrypt
- Протокол VLESS с XTLS Vision
- Идеально для домашнего интернета

### 2. **VLESS + Reality** (порт 8443) 
- Без сертификатов, использует реальные сайты как маскировку
- Лучшая обход блокировок на мобильном интернете
- Более быстрый и стелс-режим

## Функционал скрипта

✅ **Автоматическая установка:**
- Установка Xray и всех зависимостей
- Настройка сертификатов Let's Encrypt
- Генерация Reality ключей (X25519)
- Настройка фаервола (UFW)

✅ **Гибкая конфигурация:**
- Можно создавать конфиги по одному (TLS или Reality)
- Не создаёт лишние конфиги без необходимости
- Управление через удобное меню

✅ **Удобное управление:**
- Показ ссылок и QR-кодов
- Полная информация о сервере
- Перезапуск сервиса одной кнопкой
- Полное удаление со скрипта

✅ **Автоматизация:**
- Cron для обновления сертификатов
- Автоматический перезапуск Xray после обновления
- Сохранение всех настроек

---

## 📦 Установка и запуск

### 🔒 Важно: Проверьте доступ по SSH!
Скрипт откроет порты 443 и 8443, но **закроет все остальные**. Убедитесь, что порт 22 (SSH) открыт:

```bash
# Проверить статус UFW
ufw status

# Если UFW не активен или нет порта 22
ufw allow 22/tcp
ufw enable
```

### 🚀 Быстрая установка (удалённо)

```bash
sudo bash -c "$(curl -fsSL https://raw.githubusercontent.com/dzen25/vless_server_instal/refs/heads/v3/install_xray.sh)"
```

### 🛠 Локальная установка

```bash
git clone -b v3 https://github.com/dzen25/vless_server_instal.git
cd vless_server_instal
sudo bash install_xray.sh
```

---

## 🖥 Режимы работы

### 📋 Интерактивный режим (рекомендуется)

```bash
sudo bash install_xray.sh
```

Скрипт запросит:
- **Доменное имя** (должно указывать на ваш сервер)
- **Email** (для Let's Encrypt)
- **Количество начальных конфигов** (можно 0)

### ⚙ Headless режим (для автоматизации)

```bash
sudo bash install_xray.sh --headless your.domain.com your@email.com 3
```

**Параметры:**
1. Домен
2. Email для Let's Encrypt  
3. Количество начальных конфигов

---

## 📱 После установки

### Доступные команды:

#### 1. **Запуск меню управления** (основной способ)
```bash
sudo bash install_xray.sh
```

#### 2. **Быстрая генерация ссылок**
```bash
generate_client_config
```

### 🎛 Меню управления

После установки скрипт открывает меню с 6 пунктами:

```
==== XRAY МЕНЮ ====
1. Показать информацию о сервере
2. Показать ссылку/QR-код для конфига
3. Создать новый конфиг        ← Здесь выбираешь TLS или Reality!
4. Перезапустить Xray
5. Удалить Xray
6. Выйти
====================
```

---

## 🔧 Создание конфигов

### При создании нового конфига (пункт 3 в меню):

1. **Выберите тип:**
   - `1. TLS` - на порту 443 с сертификатом
   - `2. Reality` - на порту 8443 без сертификатов

2. **Автоматически:**
   - Генерируется новый UUID
   - Добавляется в серверный конфиг
   - Создаётся клиентский файл
   - Предлагается показать ссылку

### Какой протокол выбрать?

| Характеристика | TLS (443) | Reality (8443) |
|----------------|-----------|----------------|
| **Сертификаты** | Нужны (Let's Encrypt) | Не нужны |
| **Блокировки** | Чаще блокируют | Лучше обходит |
| **Скорость** | Высокая | Максимальная |
| **Сложность** | Проще | Сложнее |
| **Использование** | Домашний интернет | Мобильный интернет |

**Рекомендация:** Создайте по одному конфигу каждого типа!

---

## 📊 Показ ссылок

При выборе пункта "Показать ссылку/QR-код":

1. **Видите список доступных конфигов**
2. **Выбираете конфиг** (например: `client_1.json` или `client_2_reality.json`)
3. **Выбираете что показать:**
   - Только ссылку
   - Ссылку + QR-код
   - Полную информацию + QR-код

### Пример ссылок:

**TLS:**
```
vless://uuid@your.domain.com:443?flow=xtls-rprx-vision&security=tls&type=tcp&fp=chrome&sni=your.domain.com#TLS-1
```

**Reality:**
```
vless://uuid@your.domain.com:8443?type=tcp&security=reality&pbk=public_key&fp=chrome&sni=www.lovelawsblog.com&sid=short_id#Reality-1
```

---

## 🗑 Удаление

Для полного удаления сервера:

```bash
sudo bash install_xray.sh
```

Выберите пункт **5. Удалить Xray** в меню.

**Что удаляется:**
- Xray и его конфиги
- Наши клиентские конфиги
- Скрипты генерации

**Что остаётся:**
- Сертификаты Let's Encrypt (в `/etc/letsencrypt/`)
- Правила фаервола (порт 22 останется открыт)

---

## 📁 Структура файлов

```
/usr/local/etc/xray/config.json      # Основной конфиг сервера
/etc/xray/client_configs/            # Клиентские конфиги
/etc/xray/cert/                      # Сертификаты для TLS
/etc/xray/reality_keys               # Ключи для Reality
/usr/local/bin/generate_client_config # Скрипт генерации ссылок
/etc/xray/.installed                 # Маркер установки
/var/log/xray/install.log           # Лог установки
```

---

## 🔄 Автоматическое обновление

Скрипт автоматически настраивает:
- **Обновление сертификатов** Let's Encrypt (каждый день в 3:00)
- **Автоматический перезапуск** Xray после обновления
- **Копирование новых сертификатов** в нужную директорию

---

## ⚠️ Важные заметки

### 1. **Домен должен быть настроен заранее**
   - DNS A-запись должна указывать на IP вашего сервера
   - Проверьте: `ping your.domain.com`

### 2. **Порты**
   - **443** - TLS (может блокироваться у мобильных операторов)
   - **8443** - Reality (лучше проходит блокировки)
   - **80** - временно открывается для получения сертификата
   - **22** - должен быть открыт ДО установки

### 3. **Режимы использования**
   - **Дома/офис** - используйте TLS (порт 443)
   - **Мобильный интернет** - используйте Reality (порт 8443)
   - **При блокировках** - переключайтесь между портами

### 4. **Клиенты для подключения**
   - **Android:** v2rayNG, NekoBox
   - **iOS:** Shadowrocket, Stash
   - **Windows:** v2rayN, NekoRay
   - **macOS:** ClashX, v2rayU

---

## 🆘 Устранение проблем

### Сервер не запускается:
```bash
systemctl status xray
journalctl -u xray -n 50
```

### Проблемы с сертификатами:
```bash
certbot certificates
ls -la /etc/xray/cert/
```

### Проверка портов:
```bash
ss -tulpn | grep -E '(:443|:8443)'
```

---

## 🔄 Обновление скрипта

```bash
cd /path/to/vless_server_instal
git pull origin main
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `cat /var/log/xray/install.log`
2. Проверьте статус сервиса: `systemctl status xray`
3. Убедитесь что домен резолвится: `nslookup your.domain.com`

---

**⭐ Если скрипт был полезен - поставьте звезду на GitHub!**
```

Ключевые изменения:
1. Добавлена информация о двух протоколах (TLS и Reality)
2. Подробно описано новое меню управления
3. Объяснено как создавать конфиги по одному
4. Добавлена таблица сравнения протоколов
5. Улучшена структура для лучшей читаемости
6. Добавлены советы по выбору протокола
7. Обновлены примеры ссылок для обоих протоколов
